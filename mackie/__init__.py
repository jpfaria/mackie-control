"""Mackie Control surfaces driving any gear.

`protocol` is the wire format, `surfaces` says what each control surface has,
and `bridge` wires a surface to real gear through one driver per device."""
